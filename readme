gradeStudent
This function accepts a number (between 0 and 100) representing a student's marks and returns a string representing the corresponding grade ("A", "B", "C", "D", or "E").
Input: A number (between 0 and 100) representing the student's marks.
Return: A string representing the corresponding grade ("A", "B", "C", "D", or "E").

Purpose
Calculates a Net Salary after PAYE, NHIF, NSSF deductions, and additional Benefits from a given Gross Salary.
Inputs
Gross Salary: The gross salary of the employee, before any deductions are made.
Benefits: Any additional benefits to be added to the net salary (such as housing allowance, medical insurance, etc.).

his function calculates and returns the number of demerit points accrued for a given speed.
Input
Speed: The speed in km/s at which the driver was traveling.
Output
If the speed is less than or equal to 70 km/s, the function returns the string "Ok" to indicate that no demerits have been accrued.

